module main

var a = 25
var b : double
var arr : [int]
var arr2 = ["hola", "gaytan"]
var parent : Parent
var child : Child


class Parent {
	var a : int
	var b : double = 10 // default 
	func init(int a, double b) {
		self.a = a
		self.b = b
    }

	func talk() {
        print("parent")
    }

    func shout() {
        print("PARENT")
    }
}

class Parent -> Child {
	var c : string

	func talk() {
	    print(“child”)
    }
}

func sum(int a, int b) int {
	return a + b
}

func createParent() Parent {
	var p : Parent
	return p
}

arr = [1, 2, 3, 4]
print(a) // 25
print(arr) // [“hola”, “gaytan”]

for i in arr {
	//adopta el estilo de python, reduciendo la cantidad de tokens
}

for i in [0 to 10] {
	//iterador que va de [0] -[10], dando [1] paso a la vez
}

for c in "hola" {
	//adopta el estilo de python      
}

parent.talk()  // parent
parent.shout() // PARENT

child.talk()  // child
child.shout() // PARENT (inherited from parent)
