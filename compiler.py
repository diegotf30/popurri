import operators_tokens as op
import error_tokens as e


class Compiler(object):

    def __init__(self, name='default'):
        self.name = name


# Testing purposes
if __name__ == '__main__':
    compiler = Compiler()
